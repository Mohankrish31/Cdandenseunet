import torch
# Import your model class if it's in a separate file
# from your_model_file import CDANDenseUNet
# Instantiate the model
model = CDANDenseUNet(in_channels=3, out_channels=3, base_channels=32, growth_rate=16, num_layers=3)
# Optional: put the model on GPU if available
device = torch.device("cuda" if torch.cuda.is_available() else "cpu")
model = model.to(device)
# Save the model weights (state_dict)
torch.save(model.state_dict(), "cdandenseunet.pt")
print("Model saved as cdandenseunet.pt")
